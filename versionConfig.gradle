ext {
    repoType = "release" //发布到正式服务器
//    repoType = "snapshot" //发布到测试服务器

    useLocalLibrary = false

    var = [
            compileSdkVersion: 28,
            buildToolsVersion: "28.0.3",
            minSdkVersion    : 18,
            targetSdkVersion : 28,
            versionCode      : 100,
            versionName      : "1.0",

            slideMsgVersion  : "1.0.5",
    ]

    moduleVersion_snapshot = [
            slidemsgview: [
                    version    : "1.0.5-SNAPSHOT",
                    group      : "com.tulv",
                    groupId    : "slidemsgview",
                    type       : "aar",
                    description: "dependences lib"
            ]
    ]

    moduleVersion_release = [
            slidemsgview: [
                    version    : "1.0.5",
                    group      : "com.tulv",
                    groupId    : "slidemsgview",
                    type       : "aar",
                    description: "dependences lib"
            ]
    ]
}